setenv bootdev				0
setenv bootdev.class			mmc
setenv flash.part.bootfs.num		4
setenv flash.part.rootfs.num		4
setenv kernel.bootdev			${bootdev}
setenv force_altboot			no
setenv kernel.bricknum			0
setenv kernel.bricknum.rescue		0
setenv kernel.rescue.addr		0x08040000
